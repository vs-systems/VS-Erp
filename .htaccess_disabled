RewriteEngine On

# EXCEPCIÓN: Si la visita es para la carpeta /mkt, NO aplicar la regla de abajo
RewriteCond %{REQUEST_URI} !^/mkt/

# Regla original: Si no es /mkt y no es /dist, mandar a /dist
RewriteCond %{REQUEST_URI} !^/dist/
RewriteRule ^(.*)$ dist/$1 [L]